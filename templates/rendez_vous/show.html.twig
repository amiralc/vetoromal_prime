{% extends 'base1.html.twig' %}

{% block title %}Rendez_Vous{% endblock %}

{% block body %}
    <h1> Liste des Rendez_Vous</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ rendez_vou.id }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ rendez_vou.date |date('d-m-Y')}}</td>
        </tr>
        <tr>
            <th>Duree</th>
            <td>{{ rendez_vou.duree }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendez_vous_index') }}">back to list</a>

    <a href="{{ path('app_rendez_vous_edit', {'id': rendez_vou.id}) }}">edit</a>

    {{ include('rendez_vous/_delete_form.html.twig') }}


    <a href="{{ path('app_rendez_vous_new') }}">save</a>

{% endblock %}

